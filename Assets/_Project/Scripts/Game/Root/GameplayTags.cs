namespace UnityR3ProjectTemplate.Game.Root
{
    public static class GameplayTags
    {
        public const string RESTART = nameof(RESTART);
        public const string NEXT = nameof(NEXT);
        public const string EXIT = nameof(EXIT);

        public const string ParticlesPool = nameof(ParticlesPool);
    }
}
